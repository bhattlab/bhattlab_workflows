# specify directories
raw_reads_directory: /path/to/data
output_directory: /path/to/output

# specify parameters for TrimGalore
trim_galore:
  adaptors: illumina
  quality:  30
  start_trim: 15
  end_trim: 0
  min_read_length: 60

